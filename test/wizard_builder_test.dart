import 'package:flutter/widgets.dart';
import 'package:flutter_test/flutter_test.dart';

import 'package:wizard_builder/wizard_builder.dart';
import 'package:wizard_builder/wizard_controller.dart';
import 'package:wizard_builder/wizard_page.dart';

void main() {
//   test('initialize wizard builder with no pages throws error', () {
//     final GlobalKey<NavigatorState> navigatorKey = GlobalKey<NavigatorState>();
//     final List<WizardPage> pages = null;

//     final wizardBuilder =
//         WizardController(pages: pages);

//     expect(wizardBuilder, throwsFlutterError);
//   });

  // test('adds one to input values', () {
  //   final calculator = Calculator();
  //   expect(calculator.addOne(2), 3);
  //   expect(calculator.addOne(-7), -6);
  //   expect(calculator.addOne(0), 1);
  //   expect(() => calculator.addOne(null), throwsNoSuchMethodError);
  // });
}
